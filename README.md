# Proyecto Laboratorio de Web

## Setup

- Instalar dependencias `npm install`
- Levantar la API (directorio `/server`) mediante el comando `node server.js`
- Levantar el cliente de React mediante el comando `npm start`
